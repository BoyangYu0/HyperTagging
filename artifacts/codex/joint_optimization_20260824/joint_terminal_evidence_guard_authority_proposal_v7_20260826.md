# Joint terminal-evidence guard authority proposal v7

Status: **REVISED PROPOSAL — PENDING SEVENTH INDEPENDENT REVIEW**
Date: 2026-08-26

This document is a specification proposal only. Authority status, proposal authorization, execution authorization, submission authorization, scientific execution authorization, scheduler-call authorization, payload-access authorization, promotion authorization, recovery authorization, and feasibility-execution authorization are all false. No scheduler call, payload read, test, scientific run, submission, recovery, or feasibility run was performed.

## Fifth-review disposition

V6 implements the fifth `REVISE` result. The normative document remains the sealed v3 JSON at commit `06744aa60e5da9748a55b5ed2385322a046fdfe9`, tag `ht-joint-terminal-evidence-guard-authority-proposal-v3-20260826-v1`, SHA-256 `2be54d832c357f0b53f092c6878cfd0889a59384db0a67558356fe1a42161489`.

The v5 broad replacements of `/data_materialization`, `/teacher_statistics_schema`, `/rollout_statistics_schema`, and `/four_block_recovery_contract` are removed. The primary overlay changes metadata, adds false proposal/feasibility flags, updates the decision/blocker lists, and adds the synthetic v6 gate. A mandatory second overlay adds detailed v4 requirements at named child pointers and replaces only three incompatible v3 child strings: `before_each_block`, `warmup`, and `between_blocks`. Every other v3 descendant survives byte-exactly.

The second overlay explicitly binds:

- raw-once tensor serialization and the exact tensor/manifest keys;
- one forward per ordered teacher batch/positive-level pair and signature-covering warmups;
- physical-open records, validation-only access counts, path mediation, nonsymlink read-only inputs and exclusive non-aliasing outputs;
- frozen-dtype sigmoid/comparison/loss followed by binary64 contributions and `math.fsum`;
- rollout types, ranges, structural inequalities, positive denominators and hard per-UID validity;
- parent page-touch and immutable before/after verification around four fresh evaluator subprocesses.

The JSON is normative for every pointer, key set, constant, formula, digest input, and authorization flag.

## Additive sixth-review corrections

V7 preserves the complete sealed v6 primary overlay, v4-detail overlay, correction nodes, synthetic ceiling, false authority state, and named v3-survivor list as historical evidence. The effective survivor set removes exactly `/four_block_recovery_contract/repeated_arm_identity`, because the sixth review explicitly strengthens it; every other listed survivor remains byte-exact. A third ordered overlay makes only the sixth-review corrections:

- `predicted_tree_valid` means no predicted self-edge, no predicted directed cycle, and strictly greater mother level on every predicted edge;
- `reconstruction_valid` requires exactly `predicted_tree_valid`, zero recursive-source conflicts, at least one active daughter for every active predicted non-leaf, and a nonempty recursive leaf-source set for every active predicted leaf;
- leaf retention is defined from truth and predicted active-leaf source-set unions and their intersection; root success is defined by positive canonical truth/predicted root-Counter intersection;
- the hard text now names the exact receipt field `predicted_tree_valid`;
- rollout warmup buckets are exactly low `<=4`, medium `5..8`, and high `>8`;
- repeated arms additionally require identical numeric-receipt, dtype-contract, and target-level-plan SHAs; only timing, memory, PID, block identity/ordinal, and envelope hashes may differ;
- every runtime closure entry has exact absolute-path/device/inode/owner/mode/length/SHA fields, and the exclusive output claim is fully written and fsynced with its parent before creating the exact claimed root; all output opens are `O_NOFOLLOW` beneath that root only.

The sixth-review verbatim UTF-8 SHA-256 is `162b10be7916d59c68f35f8b06bd8b2935911d52a899d6039690c5f7d9d00a6c`.

## Corrected synthetic ceiling

Each synthetic block is conservatively bounded by:

- 4,000 measured teacher model forwards;
- 32,000 measured rollout model forwards;
- 1,024 maximum teacher-signature warmup forwards;
- 864 maximum rollout-signature warmup forwards, derived as `9 depths × 3 multiplicity buckets × 4 rollout modes × 8 levels`;
- 37,888 total model forwards: `4000 + 32000 + 1024 + 864`.

The corrected component counts, both derivation strings, and total are mandatory workload-receipt keys and inputs to the canonical workload digest. The exact synthetic ABBA names remain `q32_synth_A`, `relbias_synth_A`, `relbias_synth_B`, `q32_synth_B`. Each is a fresh process using the frozen architecture/code and actual arm config with domain-separated deterministic synthetic weights and inputs. Terminal checkpoints, UIDs, events, labels, endpoints, losses, metrics, and outcomes remain prohibited. An independently reviewed operation/byte-dominance certificate remains mandatory for excluded label-dependent branches.

Feasibility still requires end-to-end time at most 10,800 seconds, unique `.batch` MaxRSS at most 48 GiB, and peak process CUDA reserved memory at most 80% of device total, within the unchanged 14,400-second/64-GiB recovery allocation. Failure requires a reviewed resource amendment and may never truncate work.

## Preserved v3 authority

The following remain byte-exact from v3 unless the JSON names an exact child replacement: source and plan bindings; canonical bytes; causal/data receipt envelope; UID/event/normalizer/batch manifests; complete teacher/rollout schemas, formulas, denominators and empty conventions; model-state digest; bootstrap seeds/matrices/Holm primary procedure/NI rules; approved margins; descriptive diagnostics excluded from authority; timing/memory exact keys; complete hard guards; terminal `0<ElapsedRaw<=14400`; unique `.batch` `0<MaxRSS<=64 GiB`; original-job 7,200-second caps; 15-second device-wide telemetry as diagnostic only; and zero-action audit counters.

## Remaining blocker and decision

No reviewed maximum-shape manifest, corrected 37,888-forward workload receipt, synthetic implementation, or operation/byte-dominance certificate exists. No feasibility or recovery execution is authorized. Seventh independent review must accept or precisely revise this v3-plus-preserved-v6-plus-correction-overlay specification. Later promotion additionally requires accepted terminal checkpoints, authorized passing feasibility and scientific receipts, every primary/noninferiority/hard/resource gate, and explicit later promotion authority. Otherwise q32 remains selected.

## Review provenance

The fifth and sixth reviews were relayed verbatim by `/root` to `/root/recon_transaction_sol_final_audit` on 2026-08-26. Their UTF-8 SHA-256 values are `d58ec70ae809603fc7585c4a542cc4ca46972955c54dd596354d6d786994995d` and `162b10be7916d59c68f35f8b06bd8b2935911d52a899d6039690c5f7d9d00a6c`. No separately tracked reviewer artifact was present at revision time.

Disposition: **PENDING SEVENTH INDEPENDENT REVIEW**.
